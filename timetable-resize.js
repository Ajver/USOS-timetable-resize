/*
Before pasting this script in the browser console window,
one may need to type:

allow pasting

To allow pasting.
*/

const utb = document.querySelector('usos-timetable');
utb.setAttribute("start", 8);
utb.setAttribute("end", 20);
utb.style = "--timetable-row-height: 0.8rem";
